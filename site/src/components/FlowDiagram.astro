---
// 流程/序列图:客户端 Mermaid 渲染(无需构建期 headless 浏览器)。
// 用法:<FlowDiagram chart={`graph TD; A-->B`} caption="..." />
// chart 走 prop(纯文本),避免 MDX 把图源当 markdown 解析。
interface Props { chart: string; caption?: string; }
const { chart, caption } = Astro.props;
---
<figure class="flow">
  <pre class="mermaid">{chart}</pre>
  {caption && <figcaption>{caption}</figcaption>}
</figure>

<script>
  import mermaid from 'mermaid';
  mermaid.initialize({
    startOnLoad: false,
    theme: 'base',
    securityLevel: 'strict',
    themeVariables: {
      fontFamily: '"Spline Sans Mono", ui-monospace, Menlo, monospace',
      fontSize: '14px',
      background: '#f3ecdf',          // 暖纸底
      primaryColor: '#fbf6ec',        // 节点底:抬升纸面
      primaryBorderColor: '#2c5a86',  // 节点:制图蓝描边(结构)
      primaryTextColor: '#211d16',    // 节点文字(墨)
      secondaryColor: '#f1e8d8',
      tertiaryColor: '#f3ecdf',
      lineColor: '#ce4326',           // 连线:朱砂(请求在流动)
      edgeLabelBackground: '#f3ecdf',
      clusterBkg: '#f1e8d8',
      clusterBorder: '#cabb9f',
    },
  });
  const run = () => mermaid.run({ querySelector: '.mermaid' });
  if (document.readyState !== 'loading') run();
  else document.addEventListener('DOMContentLoaded', run);
</script>

<style>
  .flow {
    margin: 26px 0; padding: 16px;
    background: var(--panel); border: 1px solid var(--line);
    border-radius: var(--radius); overflow-x: auto;
    box-shadow: var(--shadow-card);
  }
  .flow .mermaid { background: none; border: none; padding: 0; text-align: center; }
  .flow figcaption {
    margin-top: 10px; text-align: center;
    font-family: var(--mono); font-size: .74rem; color: var(--ink-faint);
  }
</style>
